# ── Client data must NEVER enter version control ─────────────────────
# Exports pulled from Manager (books, receivables, retainer detail, etc.)
*.xlsx
*.xls
*.csv
~$*
# keep sample fixtures explicitly if you ever add anonymised ones:
!**/fixtures/*.sample.csv

# ── Build output (regenerated; host from here or a separate branch) ──
/dist/

# ── Tooling ──
node_modules/
.DS_Store
*.log
.env
.env.*

# local editor/agent settings
.claude/settings.local.json
